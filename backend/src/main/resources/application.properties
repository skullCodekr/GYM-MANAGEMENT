# ===== Database Config =====
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/gym_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root@1234}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Serve frontend build from static folder
spring.web.resources.static-locations=classpath:/static/

# ===== Server Config =====
server.address=0.0.0.0
server.port=${PORT:8080}

# ===== SPA Compatibility =====
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER

# ===== Spring Mail Config =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:vaibhavper001@gmail.com}
spring.mail.password=${MAIL_PASSWORD:lenz obim szgw gvzs}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true

# ===== Scheduler Config =====
spring.main.allow-bean-definition-overriding=true
